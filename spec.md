# Specification

## Summary
**Goal:** Update the demo user credentials in the BlueStone Bank app to use a new email and password.

**Planned changes:**
- Update the pre-seeded demo user email to `bryanbarton45@gmail.com` and password to `alinalaura@24` in the email/password authentication hook.

**User-visible outcome:** Users can log in with the new demo credentials (`bryanbarton45@gmail.com` / `alinalaura@24`). Previous demo credentials no longer grant access.
